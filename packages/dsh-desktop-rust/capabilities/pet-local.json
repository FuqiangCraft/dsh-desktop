{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "pet-local",
  "description": "Minimal resource bridge for the bundled pet window.",
  "windows": ["pet"],
  "platforms": ["windows", "macOS", "linux"],
  "permissions": ["pet-resource-read"]
}
